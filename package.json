{
  "name": "bugmon",
  "version": "1.0.0",
  "private": true,
  "description": "Developer monsters battling in a type-safe ecosystem",
  "scripts": {
    "sync-data": "node scripts/sync-data.js",
    "build": "node scripts/build.js",
    "build:tiny": "node scripts/build.js --no-sprites",
    "serve": "python3 -m http.server"
  }
}
